spring.application.name=noteUZ-backend
spring.config.import=optional:file:.env.local[.properties]

# Konfiguracja Bazy Danych
spring.datasource.url=${DB_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Konfiguracja Supabase / Auth
supabase.auth.url=${SUPABASE_AUTH_URL}
supabase.service_key=${SUPABASE_SERVICE_KEY}
app.jwt.cookie=${APP_JWT_COOKIE}
app.jwt.maxAge=${APP_JWT_MAX_AGE}
app.jwt.refreshCookie=${APP_JWT_REFRESH_COOKIE}
app.jwt.refreshMaxAge=${APP_JWT_REFRESH_MAX_AGE}
hcaptcha.secret_key=${HCAPTCHA_SECRET_KEY}

# ==========================================
# KONFIGURACJA E-MAIL
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Twój adres e-mail (nadawca):
spring.mail.username=noteuz.info@gmail.com
spring.mail.password=zepyclhaufdnvhio
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==========================================
# LIMIT PO??CZE? DO BAZY (Wa?ne dla Supabase)
# ==========================================
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1